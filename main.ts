let vTurno = 0
input.onGesture(Gesture.Shake, function () {
    vTurno = randint(1, 3)
    if (vTurno == 1) {
        basic.showIcon(IconNames.SmallSquare)
    } else if (false) {
    	
    } else {
    	
    }
})
